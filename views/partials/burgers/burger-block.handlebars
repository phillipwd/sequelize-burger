<li>
	{{burger_name}}

	<button class="change-burger" data-id="{{id}}" data-devour="{{devour}}">
		{{#if devour}}EAT THE BURGER{{else}}DEVOURED!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>